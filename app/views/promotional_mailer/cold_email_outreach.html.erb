<% unless @preview %>
  <p>Hi <%= @recipient_name %>,</p>
<% end %>

<% if @custom_body.present? %>
  <%= @custom_body.html_safe %>
<% else %>
  <% if @ai_generated_content.present? %>
    <%= @ai_generated_content.html_safe %>
  <% else %>
    <p>I recently analyzed 100 of the latest reviews for <%= @company_name %> and noticed a few trends that might be impacting your return rate.</p>

    <p>While customers love the <%= @positive_theme_words.first&.downcase || "service" %>, there is a recurring theme around <strong><%= @top_complaint&.downcase || "wait times" %></strong> that keeps coming up.</p>
  <% end %>

  <p>Check our free report on trends here: <%= link_to "View Full Report for #{@company_name}", report_url(@company.place&.place_actor_run_id || @company.place_id), target: '_blank' %>.</p>

  <p>Tablr converts these into wins—partners average 22% more revenue by addressing them.</p>

  <p>Open to a 15-min call to dive in?</p>

  <p>Best,<br/>
  Haider Ali<br/>
  Founder, Tablr<br/>
  <p style="font-size: 12px; color: #666; margin-top: 20px;">
    Don't want to receive these emails? <%= link_to "Unsubscribe", unsubscribe_url(email: @email), style: "color: #666;" %>
  </p>
  </p>
<% end %>